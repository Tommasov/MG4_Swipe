*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing — never commit the keystore or its credentials
keystore.properties
# Report write key — never commit it
apikeys.properties
*.jks
*.keystore
